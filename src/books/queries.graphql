type Query {
  # Book queries
  books(pagination: PaginationInput): BookConnection!
  book(id: ID!): Book

  # Author queries
  authors(pagination: PaginationInput): AuthorConnection!
  author(id: ID!): Author
}
